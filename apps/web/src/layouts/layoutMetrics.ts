/** Shared with FixedActionBar so pinned controls line up with MainLayout's side drawer (desktop) and bottom navigation (mobile). */
export const DRAWER_WIDTH = 240;
export const BOTTOM_NAV_HEIGHT = 64;
